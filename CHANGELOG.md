# Node.js Docker 版更新日志

## 1.2.0 - 2026-09-01（持续更新）

### 新增

- 新增宝塔 Nginx 域名绑定、移除域名及 SSL 证书链配置。
- 新增后端 Chromium 支付跳转和最终收款二维码截图，前端不接触初始支付地址和上游 Cookie。
- 新增支付状态自动轮询，支付成功后自动打开订单详情。
- 新增用户管理弹窗，显示当前手机号并提供退出登录。
- 新增订单详情卡密自动刷新、常见卡密格式兼容和订单使用说明展示。

### 安全与修复

- 上游接口响应改为字段白名单，支付、验证码和订单查询会话由服务端内存托管。
- 健康接口和 5xx 错误不再泄露店铺地址、环境信息或内部错误。
- 增加支付 URL SSRF 防护、只读容器 Chromium 临时目录及共享内存配置。
- 优化二维码检测速度，复用 Chromium 进程并为每笔支付创建隔离 context。
- 修复支付成功状态识别、订单详情 `dump: 1` 卡密获取和异步发卡刷新。
- 修复容器升级或重启后旧支付会话失效：自动切换到当前手机号的验证码订单授权。
- “我的订单”历史详情统一使用手机号查询授权，不再依赖旧 `payment_id`。

## 1.2.0 - 初始发布

### 新增

- 新增店铺名称浏览器标题、店铺公告弹窗和客服联系方式弹窗。
- 新增独立的“客服联系方式”入口。
- 客服弹窗支持 QQ、手机号/微信号和在线企微客服按钮。
- 公告弹窗新增“我知道了”按钮，并禁止点击遮罩关闭。
- 新增 Docker 版说明文档、接口文档和使用文档。
- 新增使用协议与免责声明，首次安装必须输入 `yes` 后才能配置。

### 调整

- 客服字段严格对应 `contact_qq`、`contact_mobile` 和 `contact_wechat`。
- 移除支付跳转域名白名单限制，支持 HTTP 和 HTTPS，并保留协议及 SSRF 安全校验。
- 完善安装时店铺链接、运行端口配置和自动开放端口逻辑。
- `shop-pro` 菜单支持重置店铺链接、重置端口、安装/升级和卸载。

## 1.1.2

- 完成 Node.js Docker 基础版本。
- 支持商品、下单、支付和订单查询流程。
- 支持 Docker Compose 和一键安装脚本。
